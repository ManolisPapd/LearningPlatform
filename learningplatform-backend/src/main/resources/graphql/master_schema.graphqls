type User {
    id: ID!
    name: String
    surname: String
    email: String
    password: String

}

type AuthData {
    id: ID!
    token: String!
    tokenExpiration: Int!
}

type Query {
    allUsers: [User]
    login(email: String!, password: String!): AuthData
}

schema {
    query: Query
}